
import 'package:flutter/material.dart';

class FavouritScreen extends StatelessWidget {
  static const String routeName = 'FavoritScreen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("fav screen", style : TextStyle(color: Colors.black))),


    );
  }
}
